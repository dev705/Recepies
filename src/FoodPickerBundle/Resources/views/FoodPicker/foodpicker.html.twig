{% extends "::base.html.twig" %}

{% block title %}FoodPicker{% endblock %}

    {% block content_header '' %}

        {% block body %}
            <div class="container-narrow">
                <div class="masthead">
                    <h3 class="muted">Where do we eat today?</h3>
                </div>
                <div class="jumbotron">
                    <p>Today we eat at<p>
                    <h1>{{ restaurant }}</h1>
                    <a class="btn btn-large btn-success" href="{{ path('foodpicker') }}">Next Suggestion!</a>
                    <p class="lead">previous suggestion: {{ previous }}</p>
                </div>

            </div>
        {% endblock %}